# Porta que a API vai rodar
PORT=3000

# URL de conexão do MySQL (que vai rodar no Docker futuramente)
# Formato: mysql://USUARIO:SENHA@HOST:PORTA/BANCO
DATABASE_URL="mysql://root:root123@localhost:3306/prev_maintenance_db"

# Chave secreta para criptografar os tokens de login (não compartilhe)
JWT_SECRET="super_secret_jwt_key_para_producao_mudar_depois"

# Fuso horário para garantir precisão nas datas
TZ="America/Sao_Paulo"